# Weekly maintenance

Recover operations, then structurally lint all mapped/indexed pages and canonical CSVs for schemas, stable/duplicate IDs, malformed data, broken links, orphans, provenance gaps, and mapping/MIME errors. Record actual inventory coverage and pagination.

Review only the configured rotating semantic page allowance for stale/unsupported claims, contradictions, assumption changes, duplicates, and useful missing connections. Record the cursor and exact page IDs; never call a bounded subset a full semantic review. Retain both evidentiary sides of contradictions and recognize temporal changes.

Apply only supported safe repairs through the canonical writer. Otherwise persist a unique proposal/intake record. Queue uncertain/deep work with shared lineage/dedup/admission rules; do not research it under maintenance. Reuse an existing unresolved follow-up key rather than create a weekly duplicate. A release check reads metadata only and never adopts code.
