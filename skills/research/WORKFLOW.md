# Evidence-backed queued research

Reserve a daily attempt slot before starting. A resume uses the same reservation; a later retry uses a new slot and observes bounded backoff/max attempts. After five reservations, select only eligible priority-0 rows with a real urgency reason; stop at ten.

Read the question, causal lineage, affected pages/projects, and already fetched relevant evidence. Search and inspect appropriate original/primary sources; distinguish reporting, syndication, and original evidence. Record retrieval/publication/update/event dates and limitations. Save source and research records before wiki updates. Explain what changed, why it matters to this instance, affected assumptions/documents, confidence, applicability limits, and a useful next step. Retain contradictions and time-scoped changes.

Use `evidence-review/WORKFLOW.md` for proposition-specific support and exact inspected locators. Consequential conclusions also load `adversarial-review/WORKFLOW.md` inside the same finite source/query allowance. Save claim, challenge and correction IDs in the research result. Reassess admission against the current TOPICS revision before execution, and respect terminal peripheral limitations.

Select at most three distinct children for the logical investigation, persisted across retries. Children inherit the minimum valid causal-parent expansion score plus the configured positive increment; 100 is valid and >100 is rejected. Urgent execution priority never resets expansion lineage. Verify evidence/wiki/index/calendar writes, then and only then remove the successful queue item and verify the queue. Report generation is independently retryable.
