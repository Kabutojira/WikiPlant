# Repository-URL bootstrap

Read root `INSTALL.md`, then perform its trust/capability gate. Gather only missing setup fields in one message and obtain one scoped setup authorization. Resolve the repository to an immutable commit and verify every allowlisted byte against `release/runtime-manifest.json` before copying.

Create/resume the deterministic installation identity under the approved Drive parent. Inspect sharing; never change it silently. Build the raw Drive layout and exact-ID map, then copy the pinned runtime snapshot. Generate one personalized private instance skill and use the host's supported private install flow. Stop at `AWAITING_HOST_INSTALL` if human installation is required. Run at most five separately journaled initialization investigations, then create/inspect one daily and one weekly native task. Store actual IDs/checkpoints. `ACTIVE_AWAITING_FIRST_RUN` is not `ACTIVE_VERIFIED`.

On every ambiguous or lost response, inspect existing scoped evidence before retrying. Never create operational GitHub state, an API-key dependency, a global router, or extra workflow skills.
