# Repository-URL bootstrap

Read root `INSTALL.md`, then perform its trust/capability gate. Gather only missing setup fields—including provider and destination—in one message and obtain one scoped setup authorization. Recommend Drive while GitHub remains pre-release. Resolve a published release and its detached manifest asset; pin repository/release ID/tag/commit/digest, then verify every allowlisted source byte at that commit before copying. The source tree's draft inventory is not an installable release manifest.

Create/resume the deterministic installation identity in exactly one approved provider. For Drive, inspect inherited sharing, build the raw layout/exact-ID map, and use strict controls when observed; only Drive may offer the explicitly accepted `best-effort-personal` permanent-lock mode. For GitHub, first prove a Work-accessible writer beyond the standard read-only ChatGPT GitHub app, bind one dedicated private non-fork repository by immutable ID, and initialize one canonical full ref with the complete skeleton/runtime in a verified commit. Missing Git object/ref/readback/scheduled capabilities returns `BLOCKED_GITHUB_WRITE_CAPABILITY`. Never request a token, mirror providers, or fall back silently.

Generate one provider-bound personalized private instance skill and use the host's supported private install flow. Stop at `AWAITING_HOST_INSTALL` if human installation is required. Run at most five separately journaled initialization investigations, then create/inspect one daily and one weekly native task. Store actual IDs/checkpoints. `ACTIVE_AWAITING_FIRST_RUN` is not `ACTIVE_VERIFIED`.

On every ambiguous or lost response, inspect existing scoped evidence before retrying. GitHub retries reconcile the durable intent with the current canonical ref and reachable commit; object creation alone is not completion. Never create an API-key dependency, GitHub Actions research runner, global router, or extra workflow skills.
