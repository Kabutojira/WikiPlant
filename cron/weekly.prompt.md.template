Invoke `${{skill_name}}` explicitly and run its pinned `weekly` workflow.

Binding: instance `{{instance_id}}`; Drive root `{{drive_root_id}}`; config `{{config_file_id}}`; map `{{drive_map_file_id}}`; runtime `{{release_id}}`; manifest SHA-256 `{{manifest_sha256}}`. Scope label: {{scope_summary}}.

Fetch complete current raw files by exact mapped ID. Validate the binding, recover operations, run full structural lint and only the configured rotating semantic page allowance, and persist findings/proposals through durable intake if exclusive canonical writes are not proved. Queue needed research under the normal lineage/dedup/budget policy; do not perform hidden deep research. A metadata-only release check may announce an update but must not adopt it. Report observed limits and leave conclusions for the next daily report. Do not rely on project uploads or remembered setup state.
